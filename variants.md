#  Варианты задания

1. **Буклов М. А.**
   (1) переменное `n=10,100,1000,10000`
   (2) `double`
   (3) по убыванию
   (4) Bubble Sort + Shaker Sort

1. **Грознецких Е. Д.**
   (1) переменное `n=10,100,1000,10000`
   (2) `long long`
   (3) по убыванию
   (4) Shell Sort (n/2,n/4,…,1) + Insertion Sort

1. **Денисов М. А.**
   (1) переменное `n=10,100,1000,10000`
   (2) `int`
   (3) по возрастанию
   (4) Heap Sort + Selection Sort

1. **Заворочаев Л. В.**
   (1) переменное `n=10,100,1000,10000`
   (2) `double`
   (3) по возрастанию
   (4) Quick Sort (Hoare partition) + Insertion Sort

1. **Ильин А. Д.**
   (1) переменное `n=10,100,1000,10000`
   (2) `long long`
   (3) по возрастанию
   (4) Quick Sort (Lomuto partition) + Bubble Sort

1. **Карнаухов М. А.**
   (1) переменное `n=10,100,1000,10000`
   (2) `int`
   (3) по убыванию
   (4) Merge Sort (top-down) + Insertion Sort

1. **Конгаа А. Н.**
   (1) переменное `n=10,100,1000,10000`
   (2) `double`
   (3) по возрастанию
   (4) Merge Sort (bottom-up) + Selection Sort

1. **Коцин А. О.**
   (1) переменное `n=10,100,1000,10000`
   (2) `long long`
   (3) по убыванию
   (4) Heap Sort + Shell Sort

1. **Ларичева Д. А.**
    (1) переменное `n=10,100,1000,10000`
    (2) `int`
    (3) по возрастанию
    (4) Shaker Sort + Selection Sort

1. **Москвин Г. Е.**
    (1) переменное `n=10,100,1000,10000`
    (2) `long long`
    (3) по возрастанию
    (4) Quick Sort (median-of-three pivot) + Insertion Sort
