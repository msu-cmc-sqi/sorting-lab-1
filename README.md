````markdown
# sorting-lab-1 — Практикум №1 «Методы сортировки» (C)

В этом задании нужно **реализовать два метода сортировки массива чисел** и провести **экспериментальное сравнение** (число сравнений и перемещений) на массивах разной длины и разного типа (упорядочен / обратный / случайный).  
Сдача производится через **GitHub Pull Request**.

## Постановка задачи

1. Реализовать **2 метода сортировки** (конкретные методы и вид сортировки — по вашему варианту).
2. Для каждого метода считать:
   - **число сравнений** элементов;
   - **число перемещений/обменов** элементов.
3. Генерировать исходные массивы отдельной функцией, по параметру типа массива:
   1) уже упорядочен  
   2) упорядочен в обратном порядке  
   3) случайный  
   4) случайный (второй прогон с другим массивом)
4. Провести сравнение на одинаковых исходных массивах для обоих методов.
5. Рассмотреть длины:
   - при динамическом выделении памяти: `n = 10, 100, 1000, 10000` (рекомендуется);
   - при фиксированном N: `n = 10, 20, 50, 100`.
6. Оформить результаты нескольких запусков в виде таблицы (средние значения).

Подробные требования и методические указания см. в PDF задания.

---

## Как сдавать (Fork → Pull Request)

### 1) Сделайте Fork
Нажмите **Fork** в правом верхнем углу репозитория и получите копию у себя в аккаунте.

### 2) Склонируйте свой форк
```bash
git clone https://github.com/<your-username>/sorting-lab-1.git
cd sorting-lab-1
````

### 3) Создайте ветку для сдачи

```bash
git checkout -b lab1
```

### 4) Добавьте решение в свою папку

**Важно:** меняйте **только** свою папку внутри `submissions/`.

Путь для сдачи:

```
submissions/<your-github-username>/lab1/
```

Рекомендуемая структура:

```
submissions/<username>/lab1/
  src/
    main.c
    sort1.c
    sort1.h
    sort2.c
    sort2.h
    gen.c
    gen.h
  report/
    report.pdf
  README.md   (опционально: как собрать/запустить)
```

Минимум, который должен быть в сдаче:

* исходники на C (`.c/.h`);
* отчёт **PDF** (`report.pdf`).
  (По требованиям задание сдаётся как код + PDF-отчёт.)

### 5) Закоммитьте и отправьте на GitHub

```bash
git add submissions/<your-github-username>/lab1
git commit -m "Add lab1 submission"
git push origin lab1
```

### 6) Откройте Pull Request

На GitHub в своём форке появится кнопка **Compare & pull request**.
Название PR: `Lab1: Фамилия Имя` (или как попросит преподаватель).

---

## Требования к программе (чек-лист)

* [ ] Каждый метод сортировки оформлен отдельной **C-функцией**.
* [ ] Генерация массива — отдельной **C-функцией** (4 режима массива).
* [ ] Программа считает **сравнения** и **перемещения** для каждого метода.
* [ ] Сравнение проводится на **одних и тех же** исходных массивах для обоих методов.
* [ ] Проведены эксперименты для нужных `n` (см. выше).
* [ ] В отчёте есть **таблица результатов** и **средние значения** по нескольким запускам.
* [ ] Код снабжён комментариями.
* [ ] В PR лежат **код + PDF отчёт + Исходник отчета TEX**.

---

## Подсказка по генерации случайных массивов

Можно использовать стандартные `rand()` и `srand(time(NULL))`:

```c
#include <stdlib.h>
#include <time.h>

// в начале main()
srand((unsigned)time(NULL));
```

Для 64-битных чисел может понадобиться несколько вызовов `rand()`.

---

## Сборка и запуск (пример)

Если у вас несколько файлов:

```bash
gcc -O2 -Wall -Wextra -std=c11 \
  submissions/<username>/lab1/src/*.c \
  -o lab1
./lab1
```

(Если используете Makefile — добавьте его в свою папку.)

---

## Политика репозитория

* Не изменяйте файлы других студентов.
* Не изменяйте структуру репозитория выше своей папки без отдельного разрешения.
* Один PR = одна сдача (доработки — коммитами в тот же PR).

```
